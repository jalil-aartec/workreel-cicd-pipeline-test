apiVersion: v1
kind: Service
metadata:
    name: jalilaartecworkreelcicdpipelinetest
spec:
    type: LoadBalancer
    ports:
    - port: 80 
    selector:
        app: jalilaartecworkreelcicdpipelinetest